import 'package:flutter/material.dart';

class AsuPage extends StatelessWidget {
  const AsuPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Pagina Grupos ASU"),
      ),
    );
  }
}
